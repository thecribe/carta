import React, { Fragment } from "react";
import Cohort from "./ui/Cohort";

const page = () => {
  return (
    <Fragment>
      <section className="w-full h-full p-8">
        <Cohort />
      </section>
    </Fragment>
  );
};

export default page;
